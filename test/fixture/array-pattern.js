const [error, data] = tryCatch(fn);
const [] = hello;

for (const [] of hello)
    x();

const [
    a,
    b,
    c,
    d,
    e,
    f,
] = abc();

const {
    versions: nodeVersions = defaultVersions,
} = options;

function transform([name, value]) {
    return {
        name,
        ...value,
    };
}